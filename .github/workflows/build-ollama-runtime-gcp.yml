name: Build and Push Ollama Runtime (GCP)

on:
  push:
    branches:
      - main
      - develop
    paths:
      - "services/ollama/**"
      - ".github/workflows/build-ollama-runtime-gcp.yml"
      - "terraform/gcp/**"
  workflow_dispatch: {}

jobs:
  build-push-ollama:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write

    env:
      GCP_REGION: us-east4

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set Environment Variables
        run: |
          TARGET_BRANCH="${GITHUB_REF#refs/heads/}"

          if [[ "$TARGET_BRANCH" == "main" ]]; then
            echo "ENV_NAME=prod" >> $GITHUB_ENV
            echo "GCP_PROJECT_ID=${{ secrets.GCP_PROJECT_ID_PROD }}" >> $GITHUB_ENV
          else
            echo "ENV_NAME=dev" >> $GITHUB_ENV
            echo "GCP_PROJECT_ID=${{ secrets.GCP_PROJECT_ID_DEV }}" >> $GITHUB_ENV
          fi

      - id: auth-dev
        name: Authenticate to Google Cloud (dev)
        if: github.ref == 'refs/heads/develop'
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER_DEV }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT_EMAIL_DEV }}

      - id: auth-prod
        name: Authenticate to Google Cloud (prod)
        if: github.ref == 'refs/heads/main'
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER_PROD }}
          service_account: ${{ secrets.GCP_SERVICE_ACCOUNT_EMAIL_PROD }}

      - name: Set up Cloud SDK
        uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker for Artifact Registry
        run: gcloud auth configure-docker ${GCP_REGION}-docker.pkg.dev --quiet

      - name: Check Artifact Registry repository
        id: repo-check
        run: |
          REPO_ID="ollama-runtime"
          echo "repo_id=$REPO_ID" >> "$GITHUB_OUTPUT"
          if gcloud artifacts repositories describe "$REPO_ID" --location "$GCP_REGION" >/dev/null 2>&1; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
            echo "Repository '$REPO_ID' already exists."
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
            echo "Repository '$REPO_ID' not found. It will be created by this workflow."
          fi

      - name: Create Artifact Registry repository
        if: steps.repo-check.outputs.exists == 'false'
        run: |
          if [[ "${{ steps.repo-check.outputs.exists }}" == "false" ]]; then
            gcloud artifacts repositories create "${{ steps.repo-check.outputs.repo_id }}" \
              --repository-format=docker \
              --location="$GCP_REGION" \
              --description="Docker repository for the Databro Ollama runtime"
          fi

      - name: Set image tags
        id: image-tags
        run: |
          IMAGE_SHA="${GCP_REGION}-docker.pkg.dev/${GCP_PROJECT_ID}/${{ steps.repo-check.outputs.repo_id }}/ollama-runtime:${GITHUB_SHA}"
          IMAGE_LATEST="${GCP_REGION}-docker.pkg.dev/${GCP_PROJECT_ID}/${{ steps.repo-check.outputs.repo_id }}/ollama-runtime:latest"
          echo "image_sha=$IMAGE_SHA" >> $GITHUB_OUTPUT
          echo "image_latest=$IMAGE_LATEST" >> $GITHUB_OUTPUT
          echo "IMAGE_SHA=$IMAGE_SHA" >> $GITHUB_ENV

      - name: Build and Push Ollama Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./services/ollama/Dockerfile
          push: true
          tags: |
            ${{ steps.image-tags.outputs.image_latest }}
            ${{ steps.image-tags.outputs.image_sha }}

      - name: Deployment summary
        run: |
          echo "## ✅ GCP Ollama Runtime Image Build Complete" >> $GITHUB_STEP_SUMMARY
          echo "- Environment: $ENV_NAME" >> $GITHUB_STEP_SUMMARY
          echo "- Project: $GCP_PROJECT_ID" >> $GITHUB_STEP_SUMMARY
          echo "- Repository: ${{ steps.repo-check.outputs.repo_id }}" >> $GITHUB_STEP_SUMMARY
          echo "- Image: $IMAGE_SHA" >> $GITHUB_STEP_SUMMARY